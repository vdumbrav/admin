# Система Квестов - Спецификация для PM/QA/Designer

Комплексная документация системы управления квестами: типы, пресеты, мульти-задачи и UI компоненты.

---

## Обзор Системы

### Что это такое

Система квестов - это платформа для создания и управления пользовательскими задачами с наградами. Поддерживает как простые задачи, так и сложные кампании из нескольких действий.

### Основные возможности

- **Одиночные квесты** - самостоятельные задачи
- **Мульти-квесты** - кампании из нескольких связанных задач
- **Пресеты** - готовые шаблоны для быстрого создания
- **Гибкие формы** - поля меняются в зависимости от типа квеста
- **Прогресс в реальном времени** - отслеживание создания задач

---

## Типы Квестов

| Тип | Что делает | Пример использования | Имеет подзадачи |
|-----|------------|---------------------|-----------------|
| **connect** | Подключение аккаунтов | "Подключи Twitter" | Нет |
| **join** | Вступление в группы | "Присоединись к Telegram каналу" | Нет |
| **like** | Лайки постов | "Лайкни твит" | Нет |
| **comment** | Комментарии | "Оставь комментарий" | Нет |
| **share** | Репосты | "Поделись постом" | Нет |
| **multiple** | Кампания действий | "Лайк + Комментарий + Репост" | **ДА** |
| **repeatable** | Ежедневные задачи | "7-дневный челлендж" | Нет |
| **external** | Посещение сайтов | "Посети партнерский сайт" | Нет |
| **referral** | Приглашения | "Пригласи 5 друзей" | Нет |
| **dummy** | Тестовые задачи | "Тестовый квест для разработки" | Нет |

### Группы квестов

- **social** - соцсети (Twitter, Discord, Telegram)
- **daily** - ежедневные задачи
- **partner** - партнерские квесты
- **referral** - реферальная программа
- **all** - основная группа (по умолчанию)

---

## Пресеты (Шаблоны)

### Что такое пресеты

Готовые шаблоны квестов с предустановленными настройками. Упрощают создание и обеспечивают единообразие.

### Доступные пресеты

#### 1. **Действие с постом** (`action-with-post`)

- **Для чего**: Кампании в Twitter (лайк + комментарий + репост)
- **Что показывает**: Поля для управления подзадачами
- **Что скрывает**: Лишние настройки
- **Автоматически**: Рассчитывает общую награду, генерирует текст кнопок

#### 2. **Подключение** (`connect`)

- **Для чего**: Связать основные соцсети (Twitter, Discord, Telegram)
- **Что показывает**: Выбор провайдера (только Twitter, Discord, Telegram)
- **Что скрывает**: URL поля, подзадачи, popup описание, переключатель повторяемости
- **Автоматически**: Настраивает OAuth интеграцию, генерирует popup описание по провайдеру

#### 3. **Присоединение** (`join`)

- **Для чего**: Вступить в группы/каналы
- **Что показывает**: URL группы, выбор провайдера, username для Telegram
- **Что скрывает**: Подзадачи, иконки
- **Автоматически**: Валидирует ссылки на группы, Connect Gate требования

#### 4. **Исследование** (`explore`)

- **Для чего**: Посещение внешних сайтов
- **Что показывает**: URL сайта, кастомные описания, иконки, кнопки, повторяемость
- **Что скрывает**: Соцсети настройки, подзадачи, карты наград
- **Автоматически**: Настраивает кнопки "Посетить", условные Connect Gate для соцсетей

#### 5. **7-дневный челлендж** (`seven-day-challenge`)

- **Для чего**: Ежедневные задачи с прогрессией наград
- **Что показывает**: Карту наград по дням (3-10 дней)
- **Что скрывает**: Подзадачи, соцсети
- **Автоматически**: Рассчитывает общую награду за период

---

## Мульти-задачи (Multiple)

### Как работает

1. Создается **главный квест** (контейнер)
2. К нему добавляются **подзадачи** (лайк, комментарий, репост)
3. Все подзадачи связаны с главным через `parent_id`
4. Подзадачи наследуют настройки от родителя

### Типы подзадач

- **like** - лайки постов
- **comment** - комментарии
- **share** - репосты
- **join** - вступления

*Примечание: тип `connect` не доступен в UI редакторе подзадач, так как предназначен только для родительских задач*

### Процесс создания

```
Пользователь заполняет форму
↓
Создается главный квест
↓
АВТОМАТИЧЕСКОЕ ПЕРЕКЛЮЧЕНИЕ в режим редактирования главного квеста
↓
Создаются подзадачи по очереди (в фоне)
↓
Показывается прогресс: "Создано 2 из 3"
↓
Готово или ошибки с возможностью повтора
```

**Важно**: После создания главного квеста пользователь сразу попадает на страницу редактирования `/quests/{id}`, где может видеть прогресс создания подзадач и управлять ими.

### Наследование от родителя

Подзадачи автоматически получают:

- Включенность (enabled)
- Платформы (web/twa)
- Провайдер (Twitter, Discord, etc.)
- Группу квеста
- Временные рамки

---

## UI Компоненты

### Форма создания квеста

#### Основные секции

1. **Выбор пресета** - выбор шаблона квеста
2. **Основная информация** - название, описание, группа
3. **Настройки типа** - зависят от выбранного пресета
4. **Подзадачи** - только для multiple типа
5. **Награды** - индивидуальные и общая сумма

#### Динамические поля

Поля появляются/скрываются в зависимости от пресета:

| Поле | action-with-post | connect | join | explore | seven-day |
|------|:----------------:|:-------:|:----:|:-------:|:---------:|
| URL поста/группы | ❌ Скрыто | ❌ Скрыто | ✅ Показано | ✅ Показано | ❌ Скрыто |
| Выбор провайдера | 🔒 Только чтение | ✅ Показано | ✅ Показано | ❌ Скрыто | ❌ Скрыто |
| Подзадачи | ✅ Показано | ❌ Скрыто | ❌ Скрыто | ❌ Скрыто | ❌ Скрыто |
| Карта наград | ❌ Скрыто | ❌ Скрыто | ❌ Скрыто | ❌ Скрыто | ✅ Показано |
| Иконка | ➕ Условно | ❌ Скрыто | ❌ Скрыто | ✅ Показано | ❌ Скрыто |

### Редактор подзадач

#### Возможности

- **Добавление задач** - кнопка "+ Добавить задачу"
- **Удаление** - кнопка удаления (IconTrash) на каждой задаче
- **Перетаскивание** - drag & drop для изменения порядка
- **Типы задач** - выпадающий список (лайк, комментарий, репост, вступления)
- **Награды** - индивидуальная настройка для каждой задачи

#### Автоматизация

- Общая награда = сумма всех подзадач
- Порядок задач = автоматическая нумерация
- Прокрутка к новой задаче при добавлении

### Прогресс создания

#### Состояния задач

- **Ожидает** - задача в очереди (IconClock)
- **Создается** - отправка на сервер (IconLoader с анимацией)
- **Готово** - успешно создана (IconCheck)
- **Ошибка** - создание не удалось (IconX)
- **Пропущена** - намеренно пропущена (IconArrowRight)

#### Действия при ошибках

- **Повторить** - попробовать создать снова
- **Пропустить** - продолжить без этой задачи
- **Отменить все** - прервать процесс

---

## Валидация и Правила

### Сильная клиентская валидация

#### 1. Форма (UI) - Первый уровень

- **Обязательные поля** - название, тип, группа
- **Форматы** - корректные URL, числа
- **Специфичные для пресета** - например, провайдер для connect
- **Уникальность квестов** - проверяется до отправки на сервер

#### 2. Бизнес-правила - Второй уровень

- **action-with-post**: должны быть подзадачи
- **connect**: обязателен провайдер, только один на провайдер
- **join**: обязателен URL группы
- **seven-day**: карта наград 3-10 дней
- **multiple**: только один квест на URL

#### 3. Сервер (API) - Резервная проверка

- Финальная проверка перед сохранением
- Проверка существования ресурсов
- Права доступа

### Автоматическое заполнение

#### Награды

- **multiple**: общая награда = сумма подзадач
- **seven-day**: общая награда = сумма всех дней

#### UI элементы

- **Текст кнопок**: генерируется по типу задач
- **Описания попапов**: по умолчанию для групп
- **Порядок задач**: автоматическая нумерация

---

## Интеграция с Backend

### API эндпоинты

- `POST /api/admin/tasks` - создание квеста
- `GET /api/admin/tasks` - список квестов
- `PUT /api/admin/tasks/:id` - обновление
- `DELETE /api/admin/tasks/:id` - удаление

### Создание мульти-задач

1. **POST** главный квест → получаем ID
2. **POST** каждую подзадачу с `parent_id`
3. Показываем прогресс: "Создано X из Y"
4. Обрабатываем ошибки индивидуально

### Формат данных

#### Главный квест

```json
{
  "title": "Кампания в Twitter",
  "type": "multiple",
  "group": "social",
  "provider": "twitter",
  "reward": 50
}
```

#### Подзадача

```json
{
  "title": "Лайкни пост",
  "type": "like",
  "parent_id": 123,
  "reward": 10,
  "order_by": 0
}
```

---

## UX Паттерны

### Позитивные сценарии

#### Быстрое создание

1. Выбрать пресет → поля подстраиваются
2. Заполнить основное → автозаполнение
3. Настроить детали → валидация в реальном времени
4. Создать → прогресс и уведомления

#### Мульти-задачи

1. Выбрать "action-with-post"
2. Добавить подзадачи через drag & drop
3. Настроить награды → автоподсчет общей
4. Создать → автоматическое переключение в режим редактирования
5. Просмотр прогресса создания подзадач на странице редактирования

### Обработка ошибок

#### Валидация формы

- **Подсветка полей** - красная рамка + текст ошибки
- **Блокировка кнопки** - "Создать" неактивна
- **Подсказки** - что нужно исправить

#### Ошибки создания

- **Частичный успех** - "Создано 2 из 3 задач"
- **Повтор ошибок** - кнопка "Повторить" для каждой
- **Альтернативы** - "Пропустить" проблемную задачу

#### Сетевые ошибки

- **Автоповтор** - 2-3 попытки с задержкой
- **Fallback UI** - заглушки вместо компонентов
- **Оффлайн режим** - сохранение черновиков

---

## Техническая Архитектура (краткая)

### Основные компоненты

- **QuestFormContainer** - главная форма
- **QuestFormFields** - динамические поля
- **ChildrenEditor** - редактор подзадач
- **MultiTaskProgress** - прогресс создания

### Состояние формы

- **React Hook Form** - управление формой
- **Zod** - схемы валидации
- **TanStack Query** - кеширование API

### Типизация

- **Строгие типы** - TypeScript для всех компонентов
- **API совместимость** - автогенерация из OpenAPI
- **Валидация схем** - Zod схемы из типов

---

## Лимиты и Ограничения

### Технические лимиты

- **Подзадачи**: максимум 10 на один квест (поддержка like, comment, share, join)
- **Длина названий**: 100 символов
- **7-дневный челлендж**: 3-10 дней
- **Награды**: 0-10000 очков
- **Типы квестов**: 10 типов включая dummy для тестирования

### UX ограничения

- **Последовательное создание**: подзадачи создаются по очереди
- **Автоматическая навигация**: после создания главного квеста происходит переход в режим редактирования
- **Браузер**: работает только в современных браузерах

### Бизнес правила

- **connect**: только один на провайдер
- **multiple**: минимум 1 подзадача
- **partner**: только для группы partner
- **Наследование**: подзадачи не могут менять ключевые настройки родителя
